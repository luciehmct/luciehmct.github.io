name: Deploy Website (gh-pages)

on:
  push:
    branches: [ main ]

# Give the runner token permission to push to the repo
permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          persist-credentials: true

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm ci

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          # If repository-level write permissions for GITHUB_TOKEN are not enabled,
          # create a personal access token (repo scope) and add it as a repository
          # secret named DEPLOY_TOKEN, then the action will use it.
          github_token: ${{ secrets.GITHUB_TOKEN }}
          personal_token: ${{ secrets.DEPLOY_TOKEN }}
          publish_dir: ./src
